# Non-interactive tiers (goal 7)

| Tier | Decision | Trigger | Scope | Secrets / MCP policy | Review path | Monitoring | Rollback | Proven run |
|---|---|---|---|---|---|---|---|---|
| **CI (GitHub Actions)** | adopted | `push` to `main`, `pull_request`, `workflow_dispatch` | `integranz/adlc-demo`; PR = build+test only, `main` = build, test, version, push, release | OIDC federated credentials (no cloud secret); `DOCKERHUB_TOKEN` + `DOCKERHUB_USERNAME` for base-image pulls; no MCP | PR review before merge; release job summary lists tags and digests | Actions run status, job summaries, `release-manifest-<v>` artifact; `gh run list/view` | images are immutable, so rollback = deploy the previous tag with `/adlc:deploy` | **2026-09-15, run 34997898117**: 0.1.12 pushed to `acradlcdemo`, tag `v0.1.12` |
| **CD (GitHub Actions, environment-gated)** | adopted | `workflow_dispatch` (tag, environment) from `/adlc:deploy` | `infra/app` Terraform apply in the `dev` environment | OIDC (`environment:dev` subject); required reviewer on the environment; deployments only from `main` | human approval in the GitHub environment before apply | run status, `/adlc:verify` evidence file | redeploy previous tag with `/adlc:deploy <previous tag> <env>` (revisions kept) | **2026-09-15, run 35017963941**: 0.1.14 deployed to `dev` after approval by Zima-Zema; smoke and independent verification in `adlc-demo/.adlc/evidence/0.1.14.md` |
| **Claude Routine** | adopted (must) | GitHub `release` event or schedule | `adlc-demo`: run `/adlc:verify` for the released tag and comment the ticket | claude.ai connectors (GitHub, Atlassian); no Azure credentials needed for verification via public URLs + GitHub API | evidence file committed on a `claude/*` branch; ticket comment | Routine run history at claude.ai/code/routines | disable the routine | pending (week 3) |
| **Cloud agents (Claude Code on the web)** | adopted | manual session start at claude.ai/code | run `/adlc:dockerize` in a clean cloud environment with the plugin auto-installed from `.claude/settings.json` | cloud environment variables, no secrets; MCP via committed `.mcp.json` | PR from the cloud session | session transcript | none needed (read/build only) | pending (week 3) |
| **Cursor Automations** | decide by 2026-09-22 | PR opened | review comment on `adlc-demo` PRs | Cursor team seat (Integrant) | PR comments | Cursor dashboard | disable automation | pending seat; dated skip otherwise |
