# Command catalog (goal 3)

Five user-invoked skills (`disable-model-invocation: true`), each with arguments, defaults and a safety note. Invoke as `/adlc:<name>` once the plugin is installed. Status: 5 of 5 implemented; 4 exercised on adlc-demo, `ticket` awaits the Jira OAuth grant.

| # | Command | Arguments | Defaults | Safety | Status | Example |
|---|---|---|---|---|---|---|
| 1 | `/adlc:dockerize` | `$0` app path or name; `--no-run`, `--compose`, `--force` | version from the configured versioning tool (`nbgv get-version -v SemVer2`), else `0.0.0-local`; local tag `<project>/<app>:<version>` | never pushes, never tags `latest`, never edits generated files, stops its containers, distroless runtime stays untouched | **implemented, exercised 2026-09-14** (`adlc-demo/api:0.1.6`) | `/adlc:dockerize apps/api` |
| 2 | `/adlc:plan` | `$0` env; `--layer foundation\|app`; `--image-tag` (app) | `dev`, `foundation`; `ARM_SUBSCRIPTION_ID` from `az account show` | plan only, never applies or formats in place; prints the human approval protocol; plan file gitignored and hook-protected | **implemented, exercised 2026-09-14** (`adlc-demo` foundation: 8 to add) | `/adlc:plan dev --layer foundation` |
| 3 | `/adlc:deploy` | `$0` tag; `$1` env; `--no-wait`, `--no-ticket` | latest green CI tag on the default branch; `github.cd_environment` | immutable semver only (hook + workflow check), every app image must exist in the registry, plan published before the human approval, never approves or re-runs on its own | **implemented, exercised 2026-09-15** (`0.1.14 → dev`, run 35017963941) | `/adlc:deploy 0.1.14 dev` |
| 4 | `/adlc:verify` | `$0` env; `$1` tag; `--no-write`, `--json` | env = `github.cd_environment`; tag = latest deployed | read-only against cloud and repo; never deploys, re-runs or edits app code; only writes `.adlc/evidence/<tag>.md`; CONFIRMED needs a positive observation | **implemented, script exercised 2026-09-16** (`dev 0.1.14`: 19/19 confirmed) | `/adlc:verify dev 0.1.14` |
| 5 | `/adlc:ticket` | `$0` create\|start\|review\|done\|comment\|show; `$1` key; `--evidence`, `--title`, `--message` | project, issue type and transitions from `.adlc/config.yaml` | requires the Atlassian MCP grant (never falls back to tokens), reads issues back before claiming anything, no duplicate creation, `done` refused while evidence has REFUTED claims, delete/manage groups stay disabled | implemented; **golden path pending** (interactive `/mcp` authorisation on integranz.atlassian.net) | `/adlc:ticket create` |
