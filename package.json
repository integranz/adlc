{
  "name": "adlc-marketplace",
  "version": "0.0.0-development",
  "private": true,
  "description": "Marketplace and source for the adlc Claude Code plugin. Versioned with semantic-release (Conventional Commits).",
  "scripts": {
    "validate:config-example": "node scripts/validate-config-example.mjs",
    "validate:plugin": "claude plugin validate ./plugins/adlc --strict && claude plugin validate . --strict",
    "release": "semantic-release"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^7.0.0",
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^11.0.1",
    "@semantic-release/github": "^12.0.9",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "conventional-changelog-conventionalcommits": "^8.0.0",
    "js-yaml": "^4.1.0",
    "semantic-release": "^25.0.9"
  },
  "engines": {
    "node": ">=22"
  }
}
